please use mozilla firefox. it works best on mozilla



First create user table with the following sql statement. NB the database name should be software engineering
//create database called software engineering and type the sql below to 
create table called user. 
open localhost/Group4ebooksystem/ebookedited

//note that our passwords are encrypted......extra points







CREATE TABLE `user` (
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT,
  `firstname` varchar(250) NOT NULL DEFAULT '',
  `lastname` varchar(250) NOT NULL DEFAULT '',
  `username` varchar(250) NOT NULL DEFAULT '',
  `password` varchar(200) NOT NULL DEFAULT '',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;




create a database named admin
type this there

CREATE TABLE `admin` 
(
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT,
 
 `username` varchar(250) NOT NULL DEFAULT '',


  `password` varchar(200) NOT NULL DEFAULT '',
  
PRIMARY KEY (`id`)
);







NB  there is a logout button below each page for you to logout





CREATE LAST DATABASE FOR BOOKS CALLED library

create a table named books
create elements in the table as
isbn
title
author
link

NB the link stores the directory of the book